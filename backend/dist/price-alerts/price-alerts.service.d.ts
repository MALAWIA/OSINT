import { AlertType, AlertStatus } from '../common/entities/price-alert.entity';
import { CreateAlertDto } from './dto/create-alert.dto';
import { UpdateAlertDto } from './dto/update-alert.dto';
export declare class PriceAlertsService {
    private alerts;
    private alertIdCounter;
    getUserAlerts(userId: string, status?: string): Promise<any[]>;
    getAlertById(alertId: string, userId: string): Promise<any>;
    createAlert(userId: string, dto: CreateAlertDto): Promise<{
        id: string;
        userId: string;
        companyId: string;
        companyName: any;
        ticker: any;
        alertType: AlertType;
        targetValue: number;
        currentValue: any;
        status: AlertStatus;
        message: string;
        notifyPush: boolean;
        notifyEmail: boolean;
        notifyInApp: boolean;
        isAutoGenerated: boolean;
        triggeredAt: any;
        expiresAt: string;
        createdAt: string;
        updatedAt: string;
    }>;
    updateAlert(alertId: string, userId: string, dto: UpdateAlertDto): Promise<any>;
    deleteAlert(alertId: string, userId: string): Promise<{
        message: string;
    }>;
    disableAlert(alertId: string, userId: string): Promise<any>;
    getTriggeredAlerts(userId: string): Promise<any[]>;
    getAlertStats(userId: string): Promise<{
        total: number;
        active: number;
        triggered: number;
        expired: number;
        disabled: number;
        autoGenerated: number;
        manual: number;
        byType: {
            priceAbove: number;
            priceBelow: number;
            percentChange: number;
            volumeSpike: number;
            newsMention: number;
            regulatoryUpdate: number;
        };
    }>;
}
